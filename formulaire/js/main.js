$(function() {
    $("#date-field").datepicker(); // initialise le champ #date-field pour qu'il utilise jquery ui
    $("#range-field").slider();
    $("#color-field").iris();// initialise le champ

});
